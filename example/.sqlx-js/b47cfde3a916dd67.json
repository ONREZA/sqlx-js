{
  "query": "SELECT id, COALESCE(bio, 'none') AS bio_or_default, length(bio) AS bio_len FROM users WHERE bio IS NOT NULL AND id = $1",
  "validation": "planned",
  "hasInline": true,
  "inlineQueries": [
    "SELECT id, COALESCE(bio, 'none') AS bio_or_default, length(bio) AS bio_len FROM users WHERE bio IS NOT NULL AND id = $1"
  ],
  "paramOids": [
    20
  ],
  "paramTypeIdentities": [
    20
  ],
  "paramTsTypes": [
    "bigint"
  ],
  "paramNullable": [
    false
  ],
  "columns": [
    {
      "name": "id",
      "typeOid": 20,
      "tsType": "bigint",
      "nullable": false
    },
    {
      "name": "bio_or_default",
      "typeOid": 25,
      "tsType": "string",
      "nullable": false
    },
    {
      "name": "bio_len",
      "typeOid": 23,
      "tsType": "number",
      "nullable": false
    }
  ],
  "hasResultSet": true,
  "inference": {
    "columns": [
      {
        "sources": [
          {
            "schema": "public",
            "table": "users",
            "column": "id",
            "notNull": true
          }
        ],
        "reason": "all source columns are NOT NULL"
      },
      {
        "sources": null,
        "reason": "the supported SQL expression is proven non-null"
      },
      {
        "sources": null,
        "reason": "the supported SQL expression is proven non-null"
      }
    ],
    "params": [
      {
        "targets": [
          {
            "kind": "predicate",
            "table": "users",
            "column": "id"
          }
        ],
        "reason": "a predicate reference requires a non-null value"
      }
    ]
  }
}